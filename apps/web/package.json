{
  "name": "@cf-one/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply cf-one --local",
    "db:migrate:remote": "wrangler d1 migrations apply cf-one --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260718.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.25.0"
  }
}
